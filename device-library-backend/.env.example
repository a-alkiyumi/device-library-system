PORT=5000

# Base URL of the frontend, used to build the return link sent in emails.
FRONTEND_URL=http://localhost:3000

# Gmail account used to send booking/return confirmation emails.
# EMAIL_PASS must be a Gmail App Password, not your regular password:
# https://myaccount.google.com/apppasswords
# If left unset, emails are logged to the console instead of sent.
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password
